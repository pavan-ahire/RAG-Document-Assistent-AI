# ChromaDB Vector Storage

This directory stores the persistent ChromaDB vector database generated during document processing.

The vector database contains:
- document embeddings
- semantic vector indexes
- retrieval metadata

These files are automatically generated at runtime and are excluded from GitHub using `.gitignore`.

This folder is kept to preserve the project structure.